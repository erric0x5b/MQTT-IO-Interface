#include <Arduino.h>

// Keep the original sketch in the project root, but compile through a
// conventional PlatformIO `src/` entrypoint so builds do not depend on
// `.ino` conversion from a UNC path.
#include "../MQTT-IO-Interface.ino"
